# Invoices & Balance: implementation and activation

The desktop SQLite database is the financial source of truth. The platform stores integer-pence invoice metadata and company balances, never PDFs. The only PDF path is an authenticated, rate-limited, no-store stream from private Google Drive files. Client access uses the current active/activated profile's `client_company_id`; all users of that company have the same financial access. Impersonation does not bypass financial authorization.

## Deploy in this order

1. Back up the Writer database. Release the desktop build only after the Windows upgrade/offline/restore smoke test. Migration 17 installs transactional queue triggers and a stable source identity. Existing records are queued automatically. Viewers never contact the billing API or publish documents.
2. From the VR Baker platform repository, apply `supabase/migrations/20260907090700_billing_publication.sql`, then `supabase/migrations/20260907120000_billing_credit_settlement.sql`, to the correct Supabase project. Both migrations are required for financial protocol 2. Keep `sync_enabled` and `client_enabled` false; schema deployment does not authorize importing customer data or activating the page.
3. Deploy `external-api` and `billing-download`. The API copies in the Hub and platform now include the same existing company/store/weekly export contract plus `billing.status`, `billing.stage`, and `billing.commit`. Keep `companies:read` and add `billing:write` to the dedicated Writer credential's existing scopes in `EXTERNAL_API_CREDENTIALS`. Do not grant this scope to browsers or Viewers. Do not rotate unrelated credentials.
4. In the intended Google Cloud project, enable Drive API and create a dedicated service account (no project-level Editor/Owner roles). Reuse the existing desktop Google project and share only the existing `VR - Management / Facturi` folder with its email as Viewer. Do not share the parent folder or `Baza de date`. Confirm existing PDFs are children of the selected folder and inherit the read permission.
5. Configure Edge Function secrets `BILLING_DRIVE_CLIENT_EMAIL`, `BILLING_DRIVE_PRIVATE_KEY` (PKCS#8 PEM, real newlines), and `BILLING_DRIVE_FOLDER_ID`. Use the secret manager/CLI env-file support, never tracked files or frontend Vite variables. A private-key file must have restrictive permissions. Clients never sign in to Google and the files stay non-public.
6. Copy the source identity displayed in Hub → Billing Settings → publication panel to `billing_control.writer_source_id`. Enable `sync_enabled` while keeping `client_enabled=false`. Use “Reîncearcă sincronizarea” in the Writer. This setting can be changed by the server operator only, not by authenticated clients.
7. Run “Asociază PDF-urile existente din Drive” in Hub. Set the existing folder ID in Hub Billing Settings; without an explicit ID, discovery accepts exactly one Facturi folder under the legacy or current management root and never creates a new invoice folder. Legacy names include `Factura_TGB-1.pdf` and separate series/number variants. Legacy matching requires exactly one filename match within the invoice folder and an identical locally saved PDF checksum. Missing/ambiguous/different documents remain unavailable; review the listed invoice IDs and regenerate their PDFs through Hub. Uploads are generated in main from authoritative invoice rows. The operator's existing readable PDF is updated in place and moved into the current client folder, preserving its ID, filename and link. Separate revision files remain the platform publication references; those technical snapshots are not overwritten. A legacy file linked by historical reconciliation is a mutable working copy until the next successful upload/publication replaces its platform reference with a revision snapshot.
8. Reconcile every company's invoice count, total, allocated payments, outstanding and credit with Hub. Confirm the queue has no errors. Check a private PDF through a test client in the staging environment, including after closing Hub, and verify a second-company account cannot access it. No page activation should precede these checks.
9. Deploy the frontend and set `client_enabled=true, activated_at=now()` in `billing_control`. The menu entry and route are enabled by this server switch. `New` expires one calendar month after `activated_at`, with month-end clamping. Do not reset `activated_at` during ordinary deployments. No synchronization timestamps or overdue balances are displayed.

## Protocol and recovery

### Entity synchronization and ordering eligibility

A company with at least one associated platform store may have orders placed by an admin, agent, standing orders, or an authorized client. Store `active=false` and lack of client login access do not remove the association or prevent invoice import. No synchronization operation activates a client account or changes platform access permissions.

Migration 18 adds `vrbaker_missing` to companies/stores and a separate nullable `stores.platform_active`. The pre-migration backup target is 18. Existing identity, invoices, payments, credits and local billing `is_active` flags are retained. Reading the entity list no longer deletes companies without stores. Possible duplicate names are explicitly flagged, never merged or remapped automatically. Reassignment of an invoiced store to another company is blocked for manual review, rather than moving historical invoices to another owner.

The full entity sync requests `include_meta=true` on the existing read-only `companies.list` and `stores.list` actions. Deploy the updated API before the desktop: old clients still receive arrays, while new clients require the versioned envelope, exact row counts, nonempty complete lists, unique IDs, explicit store activity and consistent company references. An older API, truncated response, missing scope or failed request prevents local reconciliation. The two lists are not a cross-table transactional snapshot; inconsistent relationships fail closed and require another sync.

Only this counted full export may mark an external entity as no longer present. Partial weekly/protected billing previews never infer removal. Disappeared entities remain visible as historical records, retain queued data, and are excluded from billing publication until their exact external ID reappears. Manual/unlinked entries remain available for explicit mapping review. No external records are deleted or recreated. Presence changes, upserts and the resulting local state share one transaction; the Writer and database instance are rechecked after network requests.

Verification: fixture tests cover inactive stores with billable invoices, absent and reappearing entities, cached publication exclusion, empty/partial exports, valid legacy UUIDs, duplicates, changed invoice ownership, migration rollback and local preservation. Before release, verify a Windows Writer upgrade from migration 17, admin/agent/standing-order invoices for an inactive store, entity-sync result/error messages, publication polling and recovery from an interrupted import. Keep client billing hidden until reconciliation and download checks finish. Deploy no keys, flags or production changes as part of local tests. Roll back with a compatible forward fix; older desktop builds do not understand presence markers and may retry removed companies.

### Updating an existing Drive invoice

**Superseded for the unreleased single-file implementation:** the historical two-file behavior below describes v0.1.102. The current contract, migration 20, recoverable cleanup, eventual consistency and acceptance requirements are documented in [document-drive-sync.md](document-drive-sync.md#single-invoice-pdf-contract-migration-20). New Drive PDFs use `Invoice_<number>.pdf`; no new technical snapshot is created.

- SQLite owns invoice identity, content and destination. Only the Writer may upload; the renderer supplies an invoice ID, never a Drive path or ID.
- Exact historical filenames are searched, then their folder ancestry is verified against the configured `Facturi` root. PDFs directly in that root or in older subfolders are eligible. An unrelated same-name file outside that tree is never modified. Multiple eligible copies, shortcuts, inaccessible ancestry and truncated results stop the operation with no replacement PDF created.
- The existing PDF is updated using [Drive v3 `files.update`](https://developers.google.com/workspace/drive/api/reference/rest/v3/files/update), with `addParents`/`removeParents` when relocation is necessary. The file ID and link survive. A new readable file is created only when no eligible historical file exists. No old PDFs are deleted automatically.
- Uploads for one invoice are serialized, including the authoritative read. Writer role, database connection, cancellation and document revision are rechecked before document writes and acknowledgement. Metadata, destination, size and checksum must confirm success. A response lost after creation is reconciled by exact filename discovery on the next attempt, not by blind recreation.
- Platform revision snapshots are still separate; do not point a new published snapshot at the mutable working copy. After editing, platform metadata and its new revision reference arrive through the existing publication queue; the update is not atomic across SQLite, Drive and the platform.
- Regression coverage uses an isolated Drive adapter fixture: legacy root/subfolder relocation, existing client folder, stable ID across retries, duplicates, unrelated folder, missing permission, invalid document, stale revision, checksum mismatch and concurrent saves. A real Windows Writer/Drive edit and move must still be verified before release. No live files or permissions are changed by these tests.
- Rollback: retain all documents and schema, and revert the desktop implementation only. This change adds no schema migration or API contract and requires no credential rotation. Older desktop versions will stop maintaining the readable working copy; they do not undo an already completed Drive update/move.

### Existing Writer credential: additive server configuration

When the complete original `EXTERNAL_API_CREDENTIALS` configuration is unavailable, do not reconstruct it from a subset or rotate unrelated integrations. The API supports the optional backend-only `EXTERNAL_API_BILLING_WRITER_CREDENTIAL_ID`: set it to the exact existing Writer credential ID verified in the audit log. This grants only `billing:write` in memory to that existing enabled credential. It does not change the aggregate secret, keys/hashes, expiry, rate limits, other scopes or other entries. Authentication and expiry checks still apply. Missing, unknown, malformed or multiple IDs grant no additional permissions. Never accept this setting from an HTTP request or a client.

Configure the setting only after explicit authorization. Verify the aggregate secret digest remains unchanged, `billing.status` succeeds from the intended Writer, and billing control stays disabled until import is separately approved. The Writer source ID is a separate database identity and must not be used as this credential ID. Revocation: remove the optional setting and redeploy the same current API to discard cached credentials. This removes only the additive grant; any scope explicitly present in the original aggregate remains authoritative. Keep protocol 2 and `products.invoice_prices` in all subsequent deployments.

The paused desktop may retain historical queue `last_error` messages even after a successful status request; those messages alone are not proof of an ongoing authorization failure. Use the current API audit outcome to verify the connection. Do not enable import just to clear old messages.

- Each company publication is a complete immutable snapshot split into parts of at most 50 invoices/deletion IDs. `billing.stage` stages the metadata; `billing.commit` validates company/store identities and atomically publishes the complete set with credit. Missing invoices require explicit deletion tombstones. The queue and tombstones survive restarts; financial changes and queue revisions share the same SQLite transaction.
- Requests use deterministic idempotency keys and monotonically increasing company revisions. Keep the committed parts for the current revision to reject conflicting same-revision payloads after an old desktop backup is restored. Incomplete abandoned older parts are cleaned after seven days on a later commit. Automatic deletion of Drive PDFs is not part of publication.
- Only a configured Writer source can publish. A source/association conflict or restored database older than the server's version requires operator reconciliation; never reset server revision guards just to clear the queue. Restore the latest verified Writer database or deliberately migrate the source after comparing both ledgers.
- Network/credential/permission errors retain the queue. Automatic attempts occur on startup and every five minutes. The settings action retries immediately. Client queries refresh on focus and every minute; last successful data remain visible while Hub is closed. Missing account data use an unavailable state, not a fabricated zero balance.
- Rollback: set `client_enabled=false` and `sync_enabled=false`. Keep the additive schema, SQLite data and Drive files. This hides the page and denies client reads/downloads while stopping further publication. Do not downgrade the database by deleting migration 17 tables.

## Verification

- Hub: `npm test`; `npx vite build` builds renderer/main/preload without releasing an installer. New SQLite tests cover transaction rollback, immutable retry payloads, payments/credit, invalid mappings and tombstones.
- Platform: `npx vitest run src/test/billingPage.test.tsx`; `deno test --allow-env supabase/functions/billing-download/handler.test.ts`; `deno check supabase/functions/billing-download/index.ts supabase/functions/external-api/index.ts`; `npm run build`.
- `bash scripts/test-billing-postgres.sh` starts an isolated temporary PostgreSQL cluster using synthetic company/profile fixtures; it does not use a production database URL. Requires `initdb`, `pg_ctl`, and `psql`. Tests atomic publication, incomplete/conflicting requests, old revisions, deletion, read-only access, cross-company isolation, inactive users, reassignment and the kill switch.
- Local browser verification uses synthetic data. It does not prove real Drive sharing, deployed Supabase policies, the live customer mapping, or Windows installer/upgrade behavior. These remain launch checks.

Current state: the service account, backend Drive secrets and additive financial schema are installed. The user completed Viewer sharing of the existing invoice folder. A real PDF was downloaded successfully using the service account and read-only scope, without persisting the PDF. The backend functions are deployed. Writer configuration, initial import, reconciliation and client activation remain pending.

### Recorded local results

- 178 Hub tests passed after integrating v0.1.98; renderer, main and preload builds passed. The generated PDF module was also executed in Node and produced a valid PDF from synthetic invoice rows.
- 4 new frontend tests and 8 download-handler tests passed. The isolated PostgreSQL assertions passed, including same-revision restore conflicts and mismatched store ownership.
- Desktop/mobile browser fixture: no JavaScript errors or viewport overflow; filtering and download-error retry state verified. Mobile uses invoice cards.
- Hub's full TypeScript check passes on the combined release candidate. Platform's broad checks retain an existing `appUpdate.test.ts` signature/assertion failure. Eight other suites initially needed test Supabase variables; all passed on focused reruns with synthetic configuration.
- Live read-only check: folder accessible, `canEdit=false`, PDF HTTP 200 with valid PDF signature. The database confirms both flags false, no mirrored invoices/accounts, and authenticated clients cannot execute publication or Drive-reference RPCs.

### Google Drive configuration follow-up

The existing Google Cloud project now has a dedicated enabled invoice-download service account with no project-wide roles. Its key, email and the existing invoice-root ID are configured as backend-only secrets on the verified VR Baker Supabase project. The local downloaded key has mode 0600. Folder sharing was completed by the user. A real service-account download verified folder read access and a valid PDF; edit capability is false. Google omitted the parent from the accessible metadata, so a direct parent-access denial was not tested.

The current desktop layout is `Facturi / company / Facturi`; uploads now reuse that layout. Historical reconciliation inventories the bounded folder tree and checks both legacy local PDFs and the current per-company local path. The download backend validates the actual ancestor chain against the configured invoice root; unrelated folders, shortcuts, cycles and inaccessible ancestors never permit streaming. Company authorization remains enforced independently through RLS. No existing documents were moved or deleted during this configuration.

### Backend deployment completed

The user explicitly approved production deployment of `external-api` and `billing-download`. Before deployment, the existing API was still version 11 and all tested artifact hashes matched. Deployment produced active `external-api` version 12 and `billing-download` version 1. The product ordering and delivery-zone exports from version 11 were retained. No existing secret was rotated and neither feature flag was enabled.

Six live smoke checks passed: billing-download rejects missing authentication and invalid bearer tokens with 401, rejects GET with 405, and emits private no-store headers; external-api rejects unauthenticated health, billing.status and billing.stage requests with 401. These checks do not establish successful publication from the Writer or an authenticated client invoice download, which require initial import and reconciliation. The direct service-account PDF check already passed independently of the desktop application.

Subsequent deployments: external-api version 13 added `products.invoice_prices` while preserving billing actions; version 14 retained that action and added the financial protocol 2 status response and configured invoice-folder identity. Do not redeploy the historical version 11 or 12 artifacts: they would remove client pricing used by installed Hub v0.1.98 clients.

Rollback: first keep both billing flags false and preserve the additive schema and Drive documents. Prefer a forward fix against the current API. The saved version 13 artifact is a billing-disabled containment option only after confirming its hash and preservation of `products.invoice_prices` and all other active integration contracts; it lacks the current protocol 2 status response, so it must not be used with billing synchronization enabled. The new download function always authenticates and checks RLS before contacting Drive. Release hashes, prior API source and verification receipts remain outside the repository. Remaining launch prerequisites: install the compatible Writer build after Windows checks, add billing:write to its integration credential, configure its stable source identity, backfill metadata and Drive references, reconcile all balances, then verify authenticated client downloads and enable the page.

### Current Writer compatibility and complete history

The feature branch incorporates the published Hub v0.1.98 code. The v0.1.99 candidate descends from the combined source and the pre-migration backup fix (`e59ba82`), not from an older standalone checkout. SQLite migration 17 preserves migrations 7–16 and queues every existing company. A verified pre-migration snapshot also covers upgrades from schema 16. Initial snapshots select all invoices without a date filter, including invoices issued before installation. A migration test starts with invoices dated yesterday and in the previous year and verifies both appear in the first payload. Nothing has yet been imported from the operational Writer.

Balances use the current Hub credit calculations: gross invoice total, cash paid, issued Credit Notes and applied company credit remain separate. Available credit aggregates the issuer balances. Cancelled invoices remain in history with zero outstanding and an English Cancelled status. PostgreSQL migration 20260907120000 supports those settlements; its synthetic credit/authorization tests passed. The backend schema and protocol response are deployed while both flags remain disabled. The current API's products.invoice_prices action is preserved.

Local verification: 178 desktop tests, TypeScript, both builds, PostgreSQL authorization/settlement checks, and frontend billing tests pass. The feature branch was pushed after explicit approval. Windows verification run `34111586791` passed against combined commit `9d16d01`, including installer packaging and native document checks. Final run `34111989642` also completed successfully against backup-fix commit `e59ba82`, including installer packaging. The workflow produces a temporary test installer only and does not update the stable channel.

Release decision (2026-09-07): after successful Windows CI, the user explicitly requested delivery through automatic update instead of installing the test build manually. The v0.1.99 candidate is approved for the stable workflow on that basis. This is a recorded exception to the pre-release manual smoke gate: installed Writer upgrade with retained data, offline behavior, pre-migration backup evidence and an isolated restore remain unverified and must be checked after the automatic update. Do not report those checks as passed or restore over the operational database as a test. Recheck the other worktree and remote heads before rollout to preserve any additional changes. Billing synchronization and client activation remain separate, disabled launch gates; no import is authorized by this desktop release.
